# [✓] prefix bytemapped containers with `BM_`
# [✓] autoname bytemapped structures (make name optional)
# [ ] (this requires passing the struct by name)
# [✓] add method `full?` checking that used size == size

